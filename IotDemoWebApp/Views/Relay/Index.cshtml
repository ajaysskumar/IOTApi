@model IEnumerable<IotDemoWebApp.Models.Relay>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RelayGroup.RelayGroupIpAddress)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RelayName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RelayDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RelayState)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RelayGroup.RelayGroupIpAddress)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RelayName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RelayDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RelayState)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
